// import Game from '../Game';
// import Board from '../Board';
// import ConsoleInteraction from '../cli/ConsoleInteraction';
// import CommandLineIO from '../cli/CommandLineIO';

xtest('plays a round', () => {
  // const board = new Board();
  // const game = new Game(
  //   new Board(),
  //   new ConsoleInteraction(new CommandLineIO()),
  //   new CommandLineIO()
  // );

  // game.playGame();
  // // I don't want to see console outputs when running the tests
  // // or any blocking for user input
  // // ConsoleInteractionSpec for inspiration
  // expect(game.isOver()).toEqual(true);
  // expect(board.hasWinner()).toEqual(true);
  // expect(board.grid).toEqual(['X', 'X', 'X', 'O', 'O', '', '', '', '']);
});
